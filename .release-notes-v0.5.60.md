## v0.5.60

### Overview

本版本将 Pi 平台从 Maestro 安装器中移除（Pi 改由官方 `pi-maestro-flow` 插件接入），精简 `arch-kb` 命令面为 `search/show/list` 三个子命令，并加固了一批存储标识符边界（embedding、CLI 历史、协调器报告路径、run ID、critique 存储）。产品统计范围为 `52a4778c..HEAD`，共 5 commits、101 files、+557/-375；`52a4778c` 是 v0.5.59 发布后的 closure knowhow 补录，不计入产品统计。

**Full Changelog**: https://github.com/catlog22/maestro-flow/compare/v0.5.59...v0.5.60

### Features

**安装器 Pi 平台移除**
- 从平台注册表、TUI 平台选择器与安装状态中移除 `pi` 平台，Maestro 不再向 `~/.pi/` 复制技能/Agent (`1f2d0e4`)
- 新增 `piPluginReminder` 双语提示（i18n en/zh）：平台选择器常驻提示，安装结果页在检测到 `~/.pi` 存在时显示 (`1f2d0e4`)
- 安装指南（en/zh）同步更新，引导使用 `pi install https://github.com/catlog22/pi-maestro-flow` (`1f2d0e4`)
- Hub 显示隐藏遗留的 `commands-entry` addon 行，以专用 Entry Commands 分组为唯一入口，避免重复 (`1f2d0e4`)

### Bug Fixes

- 加固存储标识符边界：embedding zvec 存储、CLI 历史存储、协调器报告路径、run ID 与 critique 存储的标识符校验与归一化，附 273 行新增测试 (`2601a3a`)

### Refactors

- `arch-kb` 命令面精简为 `search` / `show` / `list` 三个子命令，删除 `match`、`checklist`、`patterns`、`framework`、`tech-select` 五个子命令；`search --type template` 承担原 `match` 的模板匹配功能，命中后经 `show <id>`（可 `--section` 按章节）查看完整正文；同步更新 grill/blueprint/brainstorm 工作流调用与 help catalog (`bf1ee7b`)

### Compatibility Notes

- 版本保持 `0.5.x` patch 兼容，Node.js 要求不变（root 仍要求 `>=22.19.0`）。
- **Breaking（安装器）**：`maestro install` 不再提供 `pi` 平台选项；既有 `~/.pi/` 安装不受影响，Pi 集成请改用官方插件 `pi install https://github.com/catlog22/pi-maestro-flow`。
- **Breaking（CLI）**：`maestro arch-kb` 的 `match/checklist/patterns/framework/tech-select` 子命令已移除，脚本化调用需迁移到 `search` + `show`。

### Changed Files Summary

| Scope | Files | Additions | Deletions | Summary |
|---|---:|---:|---:|---|
| `src` | 23 | 285 | 266 | arch-kb 命令面精简、存储标识符加固（含测试）、安装器 Pi 平台移除与 i18n |
| `dashboard` | 2 | 148 | 10 | embedding-zvec 存储标识符测试与加固 |
| `workflows` | 3 | 16 | 27 | grill/blueprint/brainstorm 的 arch-kb 调用迁移 |
| `guide` | 2 | 14 | 1 | 安装指南 Pi 插件提示（en/zh） |
| `docs-site` | 3 | 12 | 3 | changelog 条目与版本 banner |
| `.codex` mirrors | 64 | 64 | 64 | skill frontmatter 版本戳 `0.5.60` |
| root | 2 | 3 | 3 | 版本 bump 与 lockfile 同步 |
