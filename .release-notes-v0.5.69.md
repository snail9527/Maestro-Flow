## v0.5.69

### Overview

v0.5.69 为 MaestroGraph 引入确定性的 Objective-C / Swift 跨语言结构解析管线：继承、协议遵循、扩展归属与包含关系只做**精确解析**（绝不使用模糊、后缀、路径近似或首匹配回退），通过 `SWIFT_CLASS` 识别生成式 Swift 头身份，并以可选的 `external-surfaces` 清单（项目相对路径白名单）受控接入外部 Objective-C 头文件。同时补齐查询面（hierarchy / impact / path 的结构化歧义错误与截断语义）、KG schema v8 可重放结构引用迁移、FTS 同步修复与后台同步租约加固。发布前修复了本分支在 Windows 上的规范路径回归（external-surface 身份路径未 posix 归一化导致 kg sync 的 `Invalid StructuralReference`）。

**Full Changelog**: https://github.com/catlog22/maestro-flow/compare/v0.5.68...v0.5.69

### 🚀 Features

- **确定性 ObjC/Swift 结构解析**（`src/graph/kg`，`7804f21b`）：
  - 提取 Objective-C interface / implementation / protocol / 具名 category / property / 完整 method selector，物化 contains、extends、implements、decorates 关系并保留条件编译出处
  - Swift 提取新增跨文件继承、协议遵循、扩展归属、import 与条件编译的类型化事实；`SWIFT_CLASS` 识别生成式 Swift 头身份
  - 头文件按内容分类为 C/C++/Objective-C，混合 ObjC 头保留 C 声明；现有泛型引用、诊断与非 Apple 语言行为保持不变
  - `structural_refs` 作为语法所有的事实与遗留 `unresolved_refs` 分离；边绑定 `origin_ref_key`，codegraph 替换时按事实重放 resolved / ambiguous / not_found，目标消失或回归时只重建对应边
  - 仅当恰好一个合法候选时创建结构边：重复候选保持 ambiguous、缺失目标保持 not_found，两者都不建边；ObjC 引用不解析到未显式暴露的 Swift 声明
- **受控外部表面**（`7804f21b`）：
  - 新增 opt-in `.workflow/kg/external-surfaces.json` 清单与 `maestro kg external-surfaces validate` 子命令
  - 仅接受显式项目相对头文件路径；拒绝 glob、目录、绝对路径、父级穿越、符号链接逃逸、重复 canonical 文件、超大文件与替换竞态
  - 不遍历兄弟文件、不跟随 import、不对外部表面运行提取器插件；缺失清单视为空白名单
  - 只物化被引用的 Apple catalog 条目及其声明祖先，不扫描本地 SDK
- **查询 / schema / 运行时**（`7804f21b`）：
  - 查询节点按精确 ID / 精确限定名 / 唯一简单名解析；歧义返回结构化错误而非 FTS 排名候选
  - CLI 与 MCP 对齐 hierarchy、impact、path 语义；双向路径保留原始边方向并显式报告截断
  - KG schema 推进到 v8：可重放结构引用 + 内部 FTS 同步修复；codegraph 替换、结构暂存、精确解析与泛型解析保持事务性
  - 后台同步加固：项目级租约、心跳续期、generation 围栏清理、有界 marker 读取、安全 stale 回收、私有原子冷却状态；extractor 插件 warn/fail 行为可观测并支持 fail-closed
- **Windows 规范路径对齐**（`8b5b1ed1`）：
  - external-surface `canonicalPath` 与 kg 身份路径测试期望在 win32 统一为 posix 形式，修复 Windows 上 external-surface 精确解析时 `Invalid StructuralReference: origin.filePath must be a canonical absolute` 的 kg sync 失败（产品级修复，非仅测试）

### 🐛 Bug Fixes

- **Windows kg sync 崩溃**（`8b5b1ed1`，Critical）：`collectExactFile` 的 `canonicalPath` 直接取 `realpathSync`（win32 反斜杠），与规范身份路径（全平台 posix 形式）不一致，导致结构引用校验抛错、sync 事务失败；现于清单解析返回前归一化，与 `canonicalizeCodeFilePath` 语义一致
- **测试期望平台漂移**（`8b5b1ed1`）：orchestrator / code-extractor-streaming / exact-external-file-scan / cross-language-e2e / structural-resolution-integration / external-surface-manifest 六处断言对 `realpathSync`/`join` 产物与规范身份路径直接比较，在 win32 失配；统一经 posix 归一化比较，7,515-file Pods 用例超时放宽至 30s（Windows 建文件开销）

### Changed Files Summary

| Scope | Files | Changes |
|-------|-------|---------|
| src/graph/kg | 70 | 结构解析管线（languages/index.ts、structural-reference.ts、orchestrator.ts、queries.ts、schema v8、sync 租约、external-surface manifest、CLI/MCP 查询面）、34 个测试文件与 Windows 规范路径修复 |
| src/hooks | 5 | kg-sync-hook / kg-sync-worker-state / 后台同步租约与冷却状态 |
| src/utils + src/commands | 2 | cooldown-guard 等运行时加固 |
| templates | 1 | external-surfaces.json 清单模板 |
| **Total** | **78 产品文件** | **+15418 / −1212** |

（PR #23 合并提交 `7804f21b` 为 squash，`8b5b1ed1` 为 Windows 修复提交，二者均计入产品统计；docs-site 与版本戳同步属发布期 bump 提交，不计入。）
