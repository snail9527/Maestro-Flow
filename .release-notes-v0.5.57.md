## v0.5.57

### Overview

arch-kb 隔离架构知识库 + 命令层路由收紧 + 启动/钩子性能优化 + maestro-manage 三命令拆分。新增独立于 `maestro search` 的 `maestro arch-kb` 命令（基于 awesome-architecture，80 条预构建索引）；收敛 command/prepare/workflow 三层重复表述与漂移不变量；将 maestro-manage 拆分为 issue / knowhow / knowledge 三命令；系统性清理死命令引用与文档技术债；并落地一批启动短路与惰性加载的性能优化。55 commits，305 files，+12,362/-9,584。

**Full Changelog**: https://github.com/catlog22/maestro-flow/compare/v0.5.56...v0.5.57

### 🚀 Features

**Arch-KB 架构知识库**
- 新增 `maestro arch-kb` / `akb` 隔离架构知识库命令（match/checklist/patterns/framework/list/show/search），基于 awesome-architecture (`d12223de`)
- 预构建索引 `resources/arch-kb/index.json`（80 entries：32 模板 + 42 教程 + 6 案例），独立于 `maestro search` 仅显式触发
- `installArchKb` 接入 `installAllStepContent`，注册为全局共享组件；grill/blueprint/brainstorm/claude-instructions 工作流注入 arch-kb 审查维度

**Run Contract**
- scholar 工作流补齐 Run contract 声明 (`1559e590`)

### ⚡ Performance

- `maestro -V/--version` 短路，不再 eager 注册全部命令模块 (`b9e692ac`)
- skill-context 惰性加载 run/* 模块 (`c12f970f`)
- KG hook 在廉价门禁之前不再导入 sqlite 引擎 (`e0ebe58f`)
- search-cache-invalidator 不再在工具调用路径内联重建索引 (`972fcd62`)
- coordinator-tracker 不再重复执行同一次 session 扫描 (`b061d011`)
- statusline 的 git 段加跨进程短 TTL 缓存 (`f3fe01bb`)
- resolveSelf 先查成员目录再探 git 身份 (`be03c834`)
- odyssey-improve(hook-perf)：修复 statusline 进程泄漏 (`568f5830`)、performance critical tier addressed (`d3a12341`)

### 🐛 Bug Fixes

**命令层路由与不变量**
- 收紧命令层路由与不变量表述 (`127868c1`)
- resolve 85 ambiguities across 16 maestro commands (`84974460`)
- 修复四条阻断性 CLI 死引用并打通 prepare 可达性 (`a54dc736`)
- lint 对齐 session done/next/decide CLI 迁移 + prepare/maestro 关联 (`e3b697fe`)
- 决策节点强制性归 ralph 独有 (`0418273c`)
- maestro-guard 声明 AskUserQuestion 工具 (`e991af7b`)

**Session/Run 与产物闭环**
- 修复 Session/Run 架构审计的三类一级缺陷 (`92051ad0`)
- contract 可选产物标记统一为 `required: false` (`4ff8afaa`)
- auto-test 产物发布到 run_dir/outputs (`e21d4ee8`)
- analyze 禁止写出空的 priors.json (`a092f535`)
- spec-injector 的 credibility 写入改为显式 await，不再被进程退出截断 (`fd1884f4`)

**Search / Wiki / Install**
- 消除并发 spawn 产生孤儿 search daemon 的竞态 (`3bfddc41`)
- wiki hot-path 注入不再被错误阈值全量丢弃 (`4f5a8139`)
- reinstall/upgrade 不再因不可用组件 ID 中断 (`1d99be3a`)
- 移除悬空的 chains 安装组件 (`c4dbe748`)
- 固定 search-ranking fixtures/schema/contract 为 `eol=lf`，修复 Windows clean checkout 的 qrels hash mismatch 与 contract stale (`3ccf620d`)

**镜像与引用清理**
- 修正 .codex/skills/Maestro 目录名大小写 (`17f08afe`)
- 修正 spec-setup step 名、catalog 死命令与 codex 孤儿镜像 (`e6db251f`)
- 清理 README/docs/dashboard/源码中的死命令引用 (`c4ab23eb`)
- 修正工作流中过时的 CLI 拼写 (`f5d477b1`)

### 🔧 Refactors

- maestro-manage 拆分为 issue / knowhow / knowledge 三命令 (`9e26d1e3`)，拆分收尾清理残留引用并退役 workflow (`de0a8ac1`)，完成引用迁移与 workflows 断链收口 (`57b0ebe2`)
- 收敛 command/prepare/workflow 三层的重复表述与漂移不变量 (`a22bd85f`)
- specs-add 改为意图驱动解析 (`ea67021a`)

### 📝 Documentation

- 深度更新 doc site guide 与命令描述对齐统一 Session/Run 链协议 (`21eb45a6`)，多轮深度 review 修正命令调用准确性 (`6c74d4bb`, `580650f8`)
- 解决指南结构技术债 — 收录孤儿指南 + 修复内部链接 (`0f7f4c40`)
- 重写 command-usage-guide 三份的命令全景与死引用 (`9a6eea8b`)；修正 overlay/skill-config/misc 指南的死命令与非法调用形态 (`41fe08e1`)
- 修正 reference.md 迁移表的错误映射与硬编码计数 (`d2e8c234`)；纠正 /maestro-spec 能力被写成"仅 add"与 --role/roles 死 flag (`1f98a1de`)
- 清理 guide/ 与 docs-site 剩余死命令引用与非法调用形态 (`fbc56500`, `e9005b70`)；清 odyssey 冗余 add 动词、编造的 maestro-next flag 与 v1 总览残留 (`0550c724`)
- skill 文档修正 — spec 路径迁移、reading 语义、模板解析顺序 (`cdb38235`)；修正 src/run 注释中对已删除 src/ralph 模块的现在时描述 (`18e5e5c7`)

### 🏗️ Build / Chore / Test

- 新增 build-codex-skills 镜像脚本并重生成 .codex 镜像 (`db73c28a`)；同步 maestro-issue 镜像的 description (`bef2fb37`)
- 重新生成 maestro-guard / maestro-overlay 的 codex 镜像 (`6e645140`)；同步 maestro-ralph 的 codex 镜像 (`58575d4b`)
- .pi 镜像退出版本控制 (`1a73818b`)
- 修复 session-cli built-bin 测试超时并补 dist 前置检查 (`412e47d4`)

### Changed Files Summary

| Scope | Files | Changes |
|-------|-------|---------|
| Arch-KB (new) | ~6 | 命令、索引、构建脚本、安装接线、工作流集成 |
| Commands/Routing | ~30 | 路由收紧、85 处歧义消解、死引用清理、manage 三命令拆分 |
| Session/Run | ~25 | 架构审计缺陷修复、contract 产物标记、prepare 可达性 |
| Performance | ~15 | 启动短路、惰性加载、钩子/缓存优化 |
| Codex Mirrors | ~90 | build-codex-skills 重生成、目录大小写、孤儿镜像清理 |
| Docs/Guide | ~80 | doc site 深度 review、命令全景重写、死引用清理 |
| Search/Wiki/Install | ~15 | daemon 竞态、hot-path 阈值、reinstall 中断修复 |
| Tests | ~10 | session-cli 超时、dist 前置检查 |
