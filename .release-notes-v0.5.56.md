## v0.5.56

### Overview

Maestro/Ralph 统一 Session 链协议与 Run 生命周期收敛。Maestro 与 Ralph 合并为单一 Session 链协议，引入 Skill 链提案契约（chain-proposal/1.0）与原子提案应用；新增 `session start` 人类入口命令并废弃 `run start`；重写 ralph prepare/workflow 恢复完整状态机；Session/Run 职责分离与 Ralph 完全迁移到 Run 生命周期。31 commits，242 files，+21,024/-7,783。

**Full Changelog**: https://github.com/catlog22/maestro-flow/compare/v0.5.55...v0.5.56

### 🚀 Features

**Session 链协议统一**
- 统一 Maestro 与 Ralph Session 链协议 (`8a0b29d`)
- 增加 Skill 链提案契约 chain-proposal/1.0 (`c2c8245`)
- 原子应用 Run 链提案 (`9d36c8e`)

**CLI 与 Prepare**
- 新增 `session start` 命令 + `run start` 废弃 + 全引用迁移 (`95d8d33`)
- 新建 prepare/maestro.md — 意图分类 + 建链协议 + resolvePhase (`4cdc285`)
- 重写 ralph prepare/workflow — 恢复 Stage Mapping + Build Rules + 状态机 (`1c08740`)
- maestro prepare 对齐 — flags/分解协议/建链细节/invariants (`18735d9`)

**Install**
- entry commands → skill format, hub arrow, expanded step selection (`a62aca5`)

### 🐛 Bug Fixes

**Run 生命周期闭环**
- 补全 Run 恢复闭环与建链预校验 (`aed2b05`)
- 补全 Run 自动续行与决策闭环 (`eb71468`)
- 补全 ralph decision gate 分类 + 评估流程 + 缺失环节 (`42aaf55`)
- run-executor 适配 inline-brief 正常前向流程 (`785ca0e`)

**命令引用清理**
- 全量清除残留命令引用（82 文件）(`d65713e`)
- 统一命令引用 + inline-brief 贯穿 + run start 保留 (`483b1ba`)
- 修复 Session 建链平台与终态语义 (`7b36aac`)

**其他**
- 收敛搜索排序高优先级缺口 (`a47ef10`)
- review 修复 — 废弃补全 + SKILL 源文件修正 + 死代码清理 (`430b018`)
- test 放宽 latest-verification 约束为可选+降级路由，加固 hollow-seal 回归测试 (`8293285`)

### 🔧 Refactors

- Session/Run 职责分离 + Ralph 完全迁移 (`11603cd`)
- 统一编排到 Run 生命周期 (`823ea6d`)
- 统一 Maestro 与 Ralph 编排策略 (`c63bce3`)
- 提取通用 Session 辅助命令 (`9a682ce`)
- 中立化 Run executor 与 evidence 查询 (`cf54827`)
- 移除正常路径的 Ralph CLI 调用 (`da1e44e`)

### ⚡ Performance

- brief 不再注入 run-mode.md 全文，消除执行器重复加载 (`bca1327`)

### 📝 Documentation

- 更新统一 Session Run 链架构 (`00a22cc`)
- 标记统一链迁移完成 (`b32ee16`)
- 优化代码检索工具路由 (`e401665`)
- 记录搜索排序审查问题 (`8779baf`)

### Changed Files Summary

| Scope | Files | Changes |
|-------|-------|---------|
| Session/Run core | ~40 | 链协议统一、职责分离、Run 生命周期收敛 |
| Commands/Skills | ~90 | 命令引用清理、prepare 重写、mirror 同步 |
| Ralph | ~25 | 完全迁移、decision gate、状态机恢复 |
| Install | ~15 | entry skill format、hub arrow |
| Search | ~10 | 排序缺口收敛、工具路由优化 |
| Tests | ~30 | hollow-seal 回归、latest-verification 放宽 |
| Docs/Workflows | ~30 | 架构文档、run-mode 更新 |
