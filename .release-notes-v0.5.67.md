## v0.5.67

### Overview

v0.5.67 修复 run 链式调度（chain dispatch）的参数与可达性缺口：`--arg` 现在穿透 chain 派发且失败的 session 保持 canonical-reachable，chain 启动保留 chain-file 中的步骤参数与显式 topic；同时在所有会话创建路径注册 projections、校验枚举参数，并新增 session prune 清理能力。

**Full Changelog**: https://github.com/catlog22/maestro-flow/compare/v0.5.66...v0.5.67

### 🐛 Bug Fixes

- **Chain 参数穿透**（`src/commands`、`src/config`、`src/run`）：
  - `fix(run)`: pass `--arg` through chain dispatch and keep failed sessions canonical-reachable — `--arg` 不再被 chain 派发吞掉；失败 session 通过 canonical 路径保持可达（`d2f828a7`）
  - `fix(run)`: preserve chain-file step args and explicit topic in chain start — chain 启动时保留 chain-file 内步骤声明的参数与显式 topic，避免被默认值覆盖（`4ba291b7`）
- **Session 投影与剪枝**（`src/run`、`src/commands`）：
  - `fix(run)`: register projections on all session creation paths, validate enum args, add session prune — 所有会话创建路径统一注册 projections（不再遗漏）；`--topic` 等枚举参数在校验器中显式校验；新增 session prune 支持（`ebd38837`）

### Changed Files Summary

| Scope | Files | Changes |
|-------|-------|---------|
| src/commands | 2 | run.ts（--arg 透传 + topic 保留）、session.ts（投影注册、枚举校验、prune 命令） |
| src/config | 1 | argument-hint-parser.ts（chain 参数/枚举解析修复） |
| src/run | 4 | chain-admin.ts（chain-file 参数保留）、next.ts（--arg 穿透 + 失败可达）、protocol-schemas.ts（枚举校验）、runtime.ts（投影注册 + prune） |
| **Total** | **7 产品文件** | **+218 / −25** |

（v0.5.66 closure knowhow 补录 `2ca4966b` 为独立文档提交，不计入产品统计。）
