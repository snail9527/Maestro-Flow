---
name: auto-test
description: Unified automated testing via a CSV layered pipeline, auto-selecting scenario sources from project state and iterating to convergence
argument-hint: "<phase> [-y] [-c N] [--max-iter N] [--layer L0|L1|L2|L3] [--dry-run] [--re-run]"
contract:
  consumes:
    - { kind: verification, alias: latest-verification, required: false }
    - { kind: execution, alias: current-execution, required: false }
    - { kind: review-findings, alias: latest-review, required: false }
    - { kind: diagnosis, alias: latest-debug, required: false }
  produces:
    - { path: .tests/auto-test/report.json, kind: auto-test-report, alias: latest-auto-test, role: primary }
    - { path: .tests/auto-test/test-plan.json, kind: test-plan, role: attachment }
    - { path: .tests/auto-test/state.json, kind: auto-test-state, role: evidence }
    - { path: .tests/auto-test/reflection-log.md, kind: reflection-log, role: attachment }
    - { path: .tests/auto-test/traceability.md, kind: traceability, role: evidence, optional: true }
  gates:
    exit: [tests-generated, execution-converged]
refs: []
---

# Pre-task Thinking: auto-test

## Purpose

Auto-test does unified automated testing via a CSV layered pipeline. It auto-selects scenario sources from project state (specs / coverage gaps / code exploration), then: discover → plan → build CSV → write tests (parallel) → execute → diagnose failures (parallel) → iterate → report.

Layers L0→L3 run in order (fail-fast), scenarios within a layer run in parallel. `--max-iter 1` = single-pass generation; default = full iteration cycle.

## Input Interpretation

$ARGUMENTS must provide a phase number. Routing is auto-detected from project state: an active session resumes; `--re-run` re-runs previous failures; otherwise the scenario source (spec / gap / code) is selected by priority — spec package first, then Nyquist gaps, else code exploration.

## Required Context

Pre-load (all optional, continue if missing):

1. **Test specs + tools**: `maestro load --type spec --category test` — load testing conventions (framework, patterns, naming), apply to all generated tests
2. **Coding specs**: `maestro load --type spec --category coding` — understand coding patterns to target tests precisely
3. **Role Knowledge**: browse with `maestro search --category test`, load relevant entries with `maestro load --type knowhow --id <id1> [id2...]`

## Boundaries and Invariants

- phase number is required (missing when no active session → E001)
- **Output boundary**: all workflow-artifact writes must land in `.tests/auto-test/` (test-plan.json, report.json, state.json, reflection-log.md, traceability.md), plus updates to `validation.json` / `index.json` and `.workflow/state.json` / `.workflow/issues/issues.jsonl` for registration. Source modification is limited to test files generated by this command; never modify existing production source
- **Test code isolation** — generated tests must be written to a test directory following project conventions; never modify production source to make tests pass
- **RED-GREEN integrity** — tests must first be written to fail (RED), then verified to pass against the implementation (GREEN); never write trivially-passing tests or empty assertions
- **Strict layer ordering** — L0→L3 execute in order and fail-fast; never skip a layer or run L2 before L1 passes
- **Existing tests are sacred** — never modify/delete/overwrite existing test files; new tests are added incrementally only
- **Convergence needs evidence** — pass rate and confidence score must be computed from actual execution results; never inferred without running
- **Bounded iteration** — outer iterations do not exceed `--max-iter` (default 5); inner test_defect fixes escalate after no more than 3 per scenario
- **spec source mandatory traceability** — when route=spec, every test scenario must trace back to some REQ-*.md requirement; untraceable tests are flagged

## Risk Checklist

- Is the scenario source correctly routed? Verify the auto-detected route (spec/gap/code) matches project state before generating tests.
- Are generated tests genuinely RED first? A test that passes on first write against unimplemented behavior is a false test.
- Did any layer get skipped? L0→L3 must run in order; a later layer running before an earlier passes is a fail-fast violation.
- Is iteration bounded? Confirm outer iterations respect `--max-iter` and inner fixes escalate after 3 attempts — never loop indefinitely.

## Gate Intent

- `tests-generated`: layer write-results are merged into master CSV state and temp files cleaned before execution (tests-generated gate); RED-first integrity holds and spec-source scenarios trace to a REQ.
- `execution-converged`: L0→L3 ran in order (fail-fast) and `report.json` exists before post-processing (execution-converged gate); pass rate and confidence are computed from actual runs within `--max-iter` bounds.
