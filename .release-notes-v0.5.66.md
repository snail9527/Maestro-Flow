## v0.5.66

### Overview

v0.5.66 修复 run 产物元数据扫描对行分隔格式（`.ndjson` / `.jsonl`）的支持缺口：run check/done 现在从行分隔输出的首行读取 `_meta` 契约元数据，处理带 UTF-8 BOM 的 JSON/Markdown 产物，并新增 `skipArtifactMetadataValidation` 选项将契约元数据不匹配降级为警告（必需产物、产物语法、安全路径与门禁仍然阻塞）。

**Full Changelog**: https://github.com/catlog22/maestro-flow/compare/v0.5.65...v0.5.66

### 🐛 Bug Fixes

- **Run 产物元数据**（`src/run`、`src/commands`）：
  - `fix(run)`: support line-delimited artifact metadata — `.ndjson`/`.jsonl` 输出的 `_meta` 从首行解析；JSON/Markdown 元数据剥离 UTF-8 BOM；新增 `application/x-ndjson` 媒体类型推断；`skipArtifactMetadataValidation` 可将 kind/schema/role/alias 不匹配降级为警告，`declaredPathMatches` 导出供复用（`02aacc17`）

### Changed Files Summary

| Scope | Files | Changes |
|-------|-------|---------|
| src/run | 3 | artifacts.ts（首行 `_meta`、BOM、媒体类型）+ runtime.ts（降级选项）+ artifacts/runtime 测试 |
| src/commands | 2 | run.ts、session.ts 传递 `skipArtifactMetadataValidation` |
| **Total** | **7 产品文件** | **+283 / −39**（另含 mirrors/docs） |

（v0.5.65 closure knowhow 补录 `55ae1acd` 为独立文档提交，不计入产品统计。）
