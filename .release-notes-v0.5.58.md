## v0.5.58

### Overview

本版本建立了从 Run 证据采集、知识曝光/消费记录、候选晋升到审计、协调和安全剪枝的完整知识治理闭环，同时把 Ralph 全面迁移到标准 Run Executor，并完成 Pi teammate tool 转换。产品统计范围为 `a5fb0597..1cfa6d0b`，共 29 commits、99 files、+9,367/-412；`a5fb0597` 是 v0.5.57 发布后的 closure knowhow 补录，不计入产品统计。

**Full Changelog**: https://github.com/catlog22/maestro-flow/compare/v0.5.57...v0.5.58

### Features

**知识生命周期与治理**
- 区分知识曝光与显式消费，建立可追踪的使用信号 (`cdddf6c`)
- 记录 Run 知识增量，并引入知识候选保守晋升 (`e7761d7`, `a58b57f`)
- 增加确定性知识审计、安全剪枝与回滚保证 (`f42f5c7`, `ff250d1`)
- 增加知识检索多样性与探索位，保留探索结果并精确记录曝光 (`d9b8b8f`, `7a09e25`)
- 协调 Run 与知识候选生命周期，形成知识协调和安全剪枝闭环 (`30d63e3`, `66df886`)
- 完善知识审查命令、身份闭环和协调后的治理流程 (`11a10c7`, `940794c`)

**Session/Run 与 Pi**
- 将 Pi Skill 绑定到 Session/Run 平台契约 (`5f7dafd`)
- 将 delegate 调用转换为 Pi teammate tools (`924e259`)

### Bug Fixes

- 补全 Ralph 建链与 Pi 预校验协议 (`fb375c3`)
- 加固知识晋升事务与佐证校验 (`1e82748`)
- 保证知识审计只读、剪枝可回滚 (`ff250d1`)
- 保持 Run 机器模式 stderr 纯净 (`1d1bb45`)
- 统一 KG 搜索过滤、知识身份与搜索 facets 排序 (`bf75d08`, `58dcca5`)
- 将显式加载准确归因到 active Run (`e6054ea`)
- 刷新可变 command definitions，避免 Run 使用陈旧命令 (`f2f9690`)
- spec injection 排除 deprecated entries (`ad399fc`)
- 固定认证 bootstrap 脚本为 LF，修复 Windows clean checkout 的 built release gate 字节边界失效（release preparation）

### Refactors

- Ralph 全面迁移到标准 Run Executor (`6da56b8`)

### Documentation, Tests, and Chore

- 同步知识生命周期和知识审查的 workflow/skill mirrors (`48d4dd3`, `10f9204`, `734f339`)
- 覆盖完整知识审查执行链 (`24b55c9`)
- 沉淀知识系统协同架构与协调后的生命周期架构 (`2c88176`, `804de50`)
- 忽略本地 analysis worktrees，避免开发隔离目录污染仓库状态 (`1cfa6d0`)

### Compatibility Notes

- 版本保持 `0.5.x` patch 兼容，不引入新的 Node.js 要求；仍要求 Node.js `>=22.19.0`。
- Ralph 内部执行已统一到 Run Executor；依赖旧 Ralph 内部实现细节的非公开集成应迁移到标准 Session/Run contract。
- deprecated knowledge/spec entries 不再参与 spec injection，依赖旧条目的项目应使用其 active replacement。

### Changed Files Summary

| Scope | Files | Additions | Deletions | Summary |
|---|---:|---:|---:|---|
| `src` | 51 | 8,076 | 188 | 知识生命周期、审计/协调、Run、search/KG、Pi 与测试 |
| `guide` / `docs` | 10 | 590 | 6 | 知识系统与 Session/Run 架构文档 |
| `workflows` / `.claude` / `.codex` | 20 | 356 | 180 | workflow、command 与 skill contract 同步 |
| `dashboard` | 6 | 128 | 9 | Wiki 搜索、索引与 ranking 对齐 |
| `scripts` / `prepare` | 6 | 207 | 23 | 发布门禁和 Run contract parity |
| Other | 6 | 10 | 6 | docs inventory、README 与 gitignore |
